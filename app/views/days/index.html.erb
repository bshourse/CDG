<div class="days">
  <div class="container">
    <% @days.each do |day| %>
      <button>
        <%=day.weekday %>
      </button>
      <% end %>
    <%= link_to "Back to Profile", profile_dir_path(@user), method: "get" %>
  </div>
</div>
